# Copy to .dev.vars for local `wrangler dev` (gitignored). In deployed
# environments these are set via `wrangler secret put <NAME>` instead.
JWT_SECRET=replace-with-a-long-random-string
RESEND_API_KEY=
RESEND_FROM_EMAIL=
